<script module lang="ts">
	import { defineMeta } from "@storybook/addon-svelte-csf";
    import Skeleton from "./index.svelte";
	import { sizeStyles } from "../../../styles/size";
	import { baseArgTypes } from "../../../styles/storybook";

	const argTypes = {
		...baseArgTypes,
		variant: {
			control: "select",
			options: ["default", "text", "image", "video", "card"],
			description: "The variant of the skeleton which determines its style and animation.",
			table: {
				type: { summary: "SkeletonVariant" },
				category: "Appearance",
			},
		},
		count: {
			control: "number",
			description: "The number of skeleton elements to display. This is useful for simulating lists or multiple items.",
			table: {
				type: { summary: "number" },
				category: "Content",
			},
		},
		size: {
			control: "select",
			options: sizeStyles,
			description: "The size of the skeleton, which affects its dimensions and font size.",
			table: {
				type: { summary: "Size" },
				category: "Appearance",
			},
		},
	} as const;

	const { Story } = defineMeta({
		title: "Components/Display/Skeleton",
		component: Skeleton,
		argTypes,
		parameters: {
			controls: {
				include: Object.keys(argTypes),
			},
			docs: {
				description: {
					component: "The Skeleton component is used to display a placeholder while content is loading. It accepts a `variant` prop to determine the style of the skeleton, a `count` prop to specify how many skeleton elements to display, and a `size` prop to adjust the dimensions of the skeleton."
				}
			}
		},
	});
</script>

<Story name="Default"></Story>