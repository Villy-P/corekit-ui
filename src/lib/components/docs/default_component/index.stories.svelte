<script module lang="ts">
	import { defineMeta } from "@storybook/addon-svelte-csf";
	import Button from "./index.svelte";
	import { baseArgTypes } from "../../../styles/storybook.ts";

	const argTypes = {
		...baseArgTypes,
        // Add your component's props here with appropriate controls and descriptions
	} as const;

	const { Story } = defineMeta({
		title: "Components/Default/Component", // Change this to the appropriate title for your component
		component: Button,
		argTypes,

		parameters: {
            controls: {
                include: Object.keys(argTypes),
            },
			docs: {
				description: {
                    // Change this description to explain what the component is and how to use it
					component: ""
				}
			}
        },
	});
</script>

<Story name="Default">
    <!-- Add your component's default story here -->
</Story>